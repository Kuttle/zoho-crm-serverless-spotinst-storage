{
  "name": "zoho-crm-serverless-spotinst-storage",
  "version": "1.0.0",
  "description": "The zoho crm requires that the oauth tokens be stored somewhere. spotinst has a document store that is used here.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "clean": "rimraf dist",
    "build": "tsc",
    "fresh": "rimraf ./node_modules package-lock.json && npm install",
    "carpe-diem": "npm run fresh && npm run build",
    "release:patch": "npm version patch && git push --tags",
    "release:minor": "npm version minor && git push --tags",
    "release:major": "npm version major && git push --tags"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mod35/zoho-crm-serverless-spotinst-storage.git"
  },
  "keywords": [
    "zoho",
    "crm",
    "serverless",
    "api",
    "spotinst"
  ],
  "author": "Edward Sutton",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/mod35/zoho-crm-serverless-spotinst-storage/issues"
  },
  "homepage": "https://github.com/mod35/zoho-crm-serverless-spotinst-storage#readme",
  "dependencies": {
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "@types/node": "^10.5.6",
    "typescript": "^3.0.1"
  }
}
